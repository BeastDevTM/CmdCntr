/**
 * @param {number} index - Range from (1 - 5)
 * @param {string} title - Name of the Quicklaunch entry
 * @param {string} path - Exact local system path to executable (Steam apps don't work yet)
 */

let setApp = (index, title, path) => {

}

const cmdList = [{name:"setApp", run:setApp}]
